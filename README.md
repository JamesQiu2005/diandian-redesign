# 点点 AI ≠ 一个 chatbot 频道

> 小红书 PC 端 · 点点 AI 搜索体验探索 · **Product Vision Memo**
> **核心主张：UGC 是主体，AI 是组织者。**

一个纯静态的单页叙事 —— 一份 **Product Vision Memo（产品方向探索）**，不是 PRD。它以"申请者对这个问题有独立思考"的语气，把"点点不必是一个独立频道、而可以是一种融进搜索的能力"这件事讲清楚。看的人在 2–3 分钟内能理解产品逻辑。

口吻刻意克制：诊断的锐度保留，但表达从"你们做错了"换成"我注意到现状做了这个选择，想探索另一个方向"。

---

## 页面结构（封面 + 8 个 section）

| # | Section | 作用 |
|---|---|---|
| — | 封面 Hero | 主张：点点 AI ≠ 一个 chatbot 频道；UGC 是主体，AI 是组织者 |
| 01 | 用户正在从搜索框迁移到对话框 | **大背景**。数据报告式 hero-stat（86% 周活 AI · 搜索 App 同比 −18.8% · 品类渗透率条 · 42% 点引用）建立 AI 搜索趋势共识 |
| 02 | 现状观察 | 中性但锐利的四点观察：AI 入口分散、追问入口藏在 AI 总结底部、参考帖折叠、预设问题窄化 |
| 03 | 浏览态重构 · 液态玻璃 | 把搜索框沉到底部；附"为什么放底部 / 为什么半透明"的设计决策论证（带数据） |
| 04 | 四个入口 | 对应四种用户状态的 AI 触达点 |
| 05 | 对话流里的 UGC | 核心论证：笔记卡片内联铺在对话流里（不折叠、不跳转） |
| 06 | 商业化飞轮 | 四层飞轮咬合在 UGC 数据上 |
| 07 | 如何验证 | 核心假设 / A·B 实验方向 / 用户调研方向（明确标注是方向，不是结论） |
| 08 | 关于 | 主张收束 + 联系方式 |

---

## 设计意图

| 决策 | 理由 |
|---|---|
| **数据先行的大背景（01）** | 先用第三方报告建立"AI 搜索趋势已确立、且高渗透品类正是小红书强项"的共识，再谈方案。数字用大字号 hero-stat 呈现，不堆表格。 |
| **搜索框沉到底部 · 液态玻璃** | 全案的视觉论点。瀑布流在半透明玻璃下继续流动、被柔化（`backdrop-filter`），呼应 Apple Liquid Glass —— 需要时拉起，不需要时安静待着。 |
| **问点点 ai 是可点击的模式开关** | Section 03 的玻璃搜索框里，「问点点 ai」点击后亮起浅绿，下方「猜你想搜」随之切换为点点 AI 增强的「猜你想问」（横跨护肤 / 穿搭 / 旅游 / 家居 / 健身 / 数码）—— 一套交互讲清"搜索"与"问 AI"如何共存于同一个框。 |
| **对话流内联笔记卡片** | Section 05 是核心论证。AI 回答精简，UGC 卡片直接铺在对话流里（不折叠、不跳转），这是 Perplexity / ChatGPT 都做不到的形态。 |
| **绿色 glow = 可恢复的 AI session** | 复用小红书已有的搜索历史，不另建对话管理系统。 |
| **编辑式 case-study 质感** | 暖纸张底色 + 思源宋体大标题 + 小红书红只作强调色，刻意避开"AI slop"美学（紫色渐变、过度圆角、generic emoji）。 |
| **基于现状、如实改造** | 四张参考截图被重绘为 mockup；诊断改为中性"观察"，并据实承认现状已有「问点点 ai」「AI 总结」等入口 —— 问题不在"没有 AI 入口"，而在入口分散、追问入口太深。 |

四张参考截图对应的改造：

- `main_page_reference` → Section 02 现状观察 mockup + Section 03 浏览态重构
- `Search_bar_reference` → Section 03 玻璃搜索框（问点点 ai 切换 + 猜你想搜/想问）+ 入口二历史记录 glow
- `DianDian_Chat_reference` → Section 05 对话流（把折叠的参考笔记搬进主体）
- `UGC_After_clicking…_reference` → 入口三 详情页"问点点"按钮

---

## 文件结构

```
diandian-redesign/
├── index.html      # 单页叙事结构（封面 + 8 个 section）
├── styles.css      # 全部样式（液态玻璃、瀑布流、对话流、飞轮、数据 hero-stat、验证卡片）
├── script.js       # 滚动进度 / 入场动画 / 瀑布流渲染 / 玻璃搜索框 / 问点点 AI 模式切换 / 对话演示
├── mock_data.js    # 全部 hard-coded 数据（笔记卡片 + 对话脚本 + 搜索历史 + 猜你想搜/想问）
├── pyproject.toml  # UV 项目声明（无运行时依赖，仅用于起本地预览）
└── README.md
```

技术栈：纯 HTML + CSS + Vanilla JS，无框架、无构建步骤。入场动画用 `IntersectionObserver`（`.reveal` 元素在进入视口前为透明）。封面图用 `picsum.photos`（按笔记 id 取种子，保证稳定），离线时自动降级为 CSS 渐变兜底，不影响版式。

---

## 本地预览（UV）

无需安装任何依赖，直接用 UV 起一个静态服务器：

```bash
cd diandian-redesign
uv run python -m http.server 8000
```

然后打开 <http://localhost:8000>。

> 直接双击 `index.html` 也能看，但走 `http://` 协议能保证字体与图片正常加载。

---

## 部署

### GitHub Pages
1. Settings → Pages → Source 选 `main` 分支、根目录，保存。
2. 访问 `https://JamesQiu2005.github.io/diandian-redesign/`。

### Vercel
1. `npm i -g vercel`（或用网页端 Import）。
2. 在 `diandian-redesign/` 下执行 `vercel`，框架选 **Other**，无需 build 命令、输出目录即当前目录。
3. 跟随提示完成，拿到线上链接。

> 静态站点，二选一即可；Vercel 给的预览链接更适合放进简历 / 投递。

---

## 数据来源（01 / 03 引用）

| 数据点 | 来源 | 时间 |
|---|---|---|
| 86% 消费者每周至少用一次 AI | 埃森哲《中国消费者洞察》 | 2026.05 |
| 搜索引擎类 App 月人均使用次数同比 −18.8% | 第三方数据监测 | 2026.04 |
| AI 消费决策渗透率：旅游 50 / 医疗 47 / 3C 41 / 美妆 31 | 明略科技 & 秒针营销科学院 | 2026.02 |
| 42% 用户经常 / 总是点击 AI 回答的引用链接 | 明略科技 & 秒针 | 2026.02 |
| 32.47% 期待界面"更流畅稳定"、超 90% 感知推荐偏差 | 上海消保委（3200 人样本） | 2026 |

> 页面内所有笔记、对话、搜索历史、猜你想搜/想问均为虚构示意，仅用于演示交互形态。
